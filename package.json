{
  "name": "mcp-search",
  "version": "0.9.1",
  "devDependencies": {
    "firebase-tools": "14.16.0",
    "husky": "9.1.7"
  },
  "private": true,
  "scripts": {
    "prepare": "husky",
    "deploy": "firebase deploy --force"
  }
}
